vendor/
.phpunit.cache/
composer.lock

# ...except the tools', which are applications rather than the library: a
# gate whose own version resolves fresh on every CI run can change its
# answer with no commit to blame it on.
!tools/*/composer.lock
.phpunit.result.cache
.php-cs-fixer.cache
build/
